version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: lomspassword
    ports:
      - "5438:5432"
    volumes:
      - ./internal/infrastructure/db/data:/var/lib/postgresql/data